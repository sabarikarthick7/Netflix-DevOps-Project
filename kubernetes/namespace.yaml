# =====================================================
# Netflix Clone - Kubernetes Namespace
# =====================================================
apiVersion: v1
kind: Namespace
metadata:
  name: netflix-clone
  labels:
    name: netflix-clone
    app: netflix-clone
    environment: production
    managed-by: kubectl
